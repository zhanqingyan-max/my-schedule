# 个人日程管理系统 · 第一版 PRD

> 定稿日期：2026-09-25
> 本文档是后续开发的唯一依据；与本文冲突的需求以本文最新版本为准。

## 1. 产品目标和使用场景

**目标**：为一名大学生（唯一用户）提供轻量的个人日程管理工具，把「每周课表」和「每日待办」整合为一个随时可查看的今日计划。

**使用场景**：
- 手机上（课间、通勤）快速看今天还有什么课、还有什么待办；
- 电脑上（宿舍）规划一周安排、添加/编辑日程；
- 打开即用，无需登录，无网络也能查看（PWA 离线）。

**产品原则**：单人使用、第一版最小可用、复杂度最低优先。

## 2. 整体页面框架和导航结构

- **三个一级页面**：首页（今日总览）、课表、日程。
- **导航形态**：
  - 手机端（<768px）：底部 Tab 栏（首页 / 课表 / 日程），当前页高亮；
  - 电脑端（≥768px）：顶部横向导航。
- **视觉基调**：白底 + 北航蓝主题色（约 #1f5aa8）；每门课程固定一种色块颜色（沿用教务截图色系）；第一版只做浅色模式，CSS 变量预留深色模式。
- **PWA**：提供 manifest 与 Service Worker，可添加到手机主屏/电脑桌面，离线可用。
- **校历内置**：第一学期第 1 周 = 2026-09-07（周一）；系统自动推算「今天是第几周、星期几」，用于过滤当周课程。国庆中秋假期在第 5 周（2026-10-01～10-07 期间无课，界面显示假期状态）。

## 3. 首页的内容结构

自上而下四个区块：

1. **日期头**：`M月D日 星期X · 第N周`。
2. **下一节课聚焦卡**：显示今天最近一节未开始的课程——课程名、起止时间、教室；今日课程全部结束或无课时显示对应文案（「今日课程已全部结束」/「今天没有课」）。
3. **今日时间轴**：课程与待办**按开始时间混排**为一条竖向时间轴：
   - 已结束的条目灰化；
   - 进行中的条目主题色描边高亮；
   - 未开始的条目正常显示；
   - 逾期（日期已过且未完成）的待办红色标记。
4. **空状态**：当天既无课又无待办时，显示空状态文案（如「今天没有课，也没有安排 🎈」）。

**首页交互**：
- 点时间轴上的课程节点 → 跳转课表页并打开该课详情弹窗；
- 点待办节点 → 打开日程的编辑弹层；勾选框可直接完成/取消；
- 页面可见时每分钟自动刷新时间状态；无需下拉刷新（纯本地数据）。

## 4. 每个模块的用途

| 模块 | 用途 |
|---|---|
| 首页 | 一眼看清「今天怎么过」：下一节课 + 课程/待办混排时间轴 |
| 课表 | 查看每周固定课程安排；按周切换（处理课程起止周、单双周、假期）；查看课程详情 |
| 日程 | 管理可增删改的待办事项；按 逾期/今天/未来/已完成 分组 |

## 5. 每个模块第一版最必要的数据和操作

### 5.1 数据模型

**课程（内置种子数据，随代码打包，只读）**：

```
Course {
  id, name, teacher?, color,
  dayOfWeek: 1-7,
  startPeriod, endPeriod,          // 节次，1-14 节
  startWeek, endWeek,              // 起止周
  classroom,
  note?                            // 如"多节课待展开"
}
```

节次时间（内置）：P1 08:00–08:45，P2 08:50–09:35，P3 09:50–10:35，P4 10:40–11:25，P5 11:30–12:15，P6 14:00–14:45，P7 14:50–15:35，P8 15:50–16:35，P9 16:40–17:25，P10 17:30–18:15，P11 19:00–19:45，P12 19:50–20:35，P13 20:40–21:25，P14 21:30–22:15。

**待办（localStorage，键按设备独立存储）**：

```
Todo {
  id, title(必填), date(YYYY-MM-DD, 必填),
  startTime?, endTime?,            // 可选；结束时间留空 = 仅一个时间点
  note?, done: bool,
  createdAt, updatedAt
}
```

### 5.2 操作

| 模块 | 第一版操作 |
|---|---|
| 首页 | 只读展示；勾选完成待办；跳转课表/日程 |
| 课表 | 上一周/下一周切换 +「回到本周」；手机端「周视图/单日视图」切换、左右滑切日；点课程 → 详情弹窗（名称、教室、节次时间、上课周次） |
| 日程 | 新建（底部弹层/居中表单：标题、日期默认今天、时间、备注）；编辑（点条目复用同一弹层）；删除（编辑态内删除 + 二次确认）；勾选完成/取消完成；分组浏览（逾期/今天/未来 7 天/更早或无时间/已完成默认折叠） |

### 5.3 已录入课表（依据 2026-09-25 用户截图）

常规周课表：

| 时间 | 课程 | 地点 |
|---|---|---|
| 周二 1–2 节 | 数学基础 | 科研一号楼 |
| 周二 3–4 节 | 「多节课」（内容待用户补充） | — |
| 周一 6–7 节 / 周二 6–7 节 / 周三 3–4 节 | 综合法语(1) | 教学一号楼 |
| 周四 3–5 节 | 综合法语实训(1)（第 5 节暂按连堂录入，待确认） | 教学二号楼 |
| 周二 8–9 节 | 体育(1) | 杭州田径场 |
| 周四 6–9 节 | 习近平新时代中国特色社会主义思想概论（暂用全称，待确认） | 科研一号楼 |
| 周五 1–2 节 | 航空航天概论A | 教学一号楼 |
| 周五 4–5 节 | 基础英语(1) | 教学一号楼 |
| 周三或周四 11–12 节 | 中国近现代史纲要（暂用全称，具体星期待确认） | 科研一号楼 |
| 周四 11–12 节 | 综合法语(1) | 教学一号楼 |

带起止周的课程（默认值，**以用户教务系统为准，待确认**）：

| 课程 | 时间 | 暂录周次 |
|---|---|---|
| 工程认识 | 周一 2–5 节 | 第 2–4 周 |
| 大学计算机基础 | 周一 8–10 节、周五 8–10 节 | 第 4–12 周 |

## 6. 首页、今日计划和各模块之间的关系

- **「今日计划」不是第四个页面**，而是首页对两个数据源的实时聚合视图：
  - 课程侧：课表种子数据 ×（今天日期 → 周次 + 星期几）过滤得出今日课程；
  - 待办侧：localStorage 中 date = 今天的待办。
- 二者按开始时间排序合并成首页时间轴，即「今日计划」。
- 首页是课表与日程的**只读消费方**：不存任何独立数据；在日程页增删改待办后，首页时间轴同步变化；寒暑假/假期周首页自然显示无课状态。
- 跳转关系：首页课程节点 → 课表详情弹窗；首页待办节点 → 日程编辑弹层。

## 7. 第一版必须完成的功能

1. 三个页面（首页/课表/日程）+ 响应式布局（375px 手机 ~ 1440px 桌面均正常）。
2. 校历内置与自动周次推算（第 1 周 = 2026-09-07）。
3. 第 5.3 节课表数据完整录入并可按周次正确显示/隐藏（含假期周）。
4. 待办的新建、编辑、删除（带确认）、完成勾选，localStorage 持久化，重进页面数据仍在。
5. 首页今日时间轴（课程+待办混排、状态区分、下一节课卡片、空状态）。
6. PWA：可安装、离线打开、断网时功能完整可用。

## 8. 第一版暂时不做的功能

- 用户注册/登录（仅个人使用）；
- 社交/分享功能；
- 系统推送通知、浏览器提醒（打开看即可）；
- 跨设备数据同步（待办各设备独立；课表因内置所以两端一致）；
- 课程调课/停课单日覆盖、导入教务系统、课表编辑；
- 待办分类/标签、优先级、重复待办、子任务；
- 深色模式、多语言、桌面小组件；
- 滑动/长按删除等待办手势操作。

## 9. 可实际检查的验收标准

**功能**
- [ ] 首次访问三个页面，无需登录，无报错（浏览器控制台无 error）。
- [ ] 在 2026-09-28（第 4 周周一）打开首页：时间轴含「大学计算机基础 08:00? 见节次」等当日课程与当日待办，按开始时间升序混排。
- [ ] 在 2026-10-05（第 5 周假期）打开课表：显示假期状态，无课程色块。
- [ ] 课表切换到第 3 周：显示「工程认识」（周一 2–5 节）、不显示「大学计算机基础」；切到第 4 周相反。
- [ ] 新建一条今天 19:00 的待办 → 立即出现在日程「今天」分组和首页时间轴 19:00 位置；刷新/关闭重开后仍存在。
- [ ] 勾选完成 → 条目进入「已完成」（默认折叠），首页时间轴显示完成态；编辑可改所有字段；删除有二次确认且列表与首页同步消失。
- [ ] 修改设备系统日期到未来某工作日，首页/课表按该日对应周次正确渲染。

**形态**
- [ ] Chrome DevTools 手机视图 375px 与桌面 1440px 下无横向滚动、无内容遮挡；底部 Tab（手机）/顶部导航（桌面）正确切换。
- [ ] 手机端课表支持周视图与单日视图切换，单日视图可左右滑切日。
- [ ] Lighthouse（或等价方式）PWA 检查通过：有 manifest、Service Worker 注册成功、可触发安装。
- [ ] 断网（DevTools offline 或飞行模式）后重新打开页面：课表、已有待办、首页时间轴全部正常显示。

**数据**
- [ ] 清空浏览器 localStorage 后，课表仍然完整显示（课表为内置数据，不依赖 localStorage）。
- [ ] 待办数据仅存于本机 localStorage，无任何网络请求发送用户数据。

---

## 附：待用户确认事项（不阻塞开发，数据可后补）

1. 「多节课」（周二 3–4 节）的具体课程与单双周安排；
2. 周四第 5 节红色块课程（暂按综合法语实训连堂处理）；
3. 《习近平新时代中国特色社会主义思想概论》《中国近现代史纲要》两课全称及「中国…」课的具体星期（周三/周四 11–12 节）；
4. 「工程认识」「大学计算机基础」的准确起止周。
